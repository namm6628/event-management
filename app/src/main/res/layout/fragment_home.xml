<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Ô tìm kiếm -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="12dp"
        app:cardBackgroundColor="@color/chip_bg"
        app:cardCornerRadius="100dp"
        app:cardElevation="0dp"
        app:cardUseCompatPadding="false"
        app:rippleColor="@android:color/transparent"
        app:strokeColor="@android:color/transparent"
        app:strokeWidth="0dp">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/searchView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/sp_gray"
            android:queryHint="Tìm sự kiện, nghệ sĩ, địa điểm..."
            app:iconifiedByDefault="false"
            app:queryBackground="@android:color/transparent"
            app:submitBackground="@color/sp_green_dark" />
    </com.google.android.material.card.MaterialCardView>


    <!-- Nhóm chip lọc nhanh -->
    <!-- ... phía trên SearchView ... -->

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:overScrollMode="never"
        android:scrollbars="none">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingEnd="4dp"
            app:selectionRequired="false"
            app:singleSelection="true">

            <com.google.android.material.chip.Chip
                style="@style/App.FilterChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:checked="true"
                android:text="Tất cả" />

            <com.google.android.material.chip.Chip
                style="@style/App.FilterChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="Âm nhạc" />

            <com.google.android.material.chip.Chip
                style="@style/App.FilterChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="Hội thảo" />

            <com.google.android.material.chip.Chip
                style="@style/App.FilterChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="Sân khấu" />

            <com.google.android.material.chip.Chip
                style="@style/App.FilterChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="Thể thao" />
        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <!-- ... phía dưới RecyclerView ... -->


    <!-- Danh sách sự kiện -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvEvents"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1" />
</LinearLayout>
