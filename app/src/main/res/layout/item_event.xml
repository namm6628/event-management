<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="240dp"
    android:layout_height="160dp"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackground">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- ẢNH FULL COVER -->
        <ImageView
            android:id="@+id/ivThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/app_name" />

        <!-- GRADIENT ĐEN MỜ DƯỚI ẢNH -->
        <View
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_gravity="bottom"
            android:background="@drawable/bg_bottom_gradient" />

        <!-- TÊN SỰ KIỆN ĐÈ LÊN ẢNH -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="12dp"
            android:text="Tên sự kiện"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="15sp"
            android:maxLines="2"
            android:ellipsize="end"
            android:background="@drawable/bg_event_title" />


        <!-- NÚT "XEM CHI TIẾT" NHỎ (nếu cần bật sau thì để đây luôn) -->
        <TextView
            android:id="@+id/btnSeeMore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="12dp"
            android:padding="6dp"
            android:text="Xem chi tiết"
            android:textSize="11sp"
            android:textColor="@android:color/white"
            android:background="@drawable/bg_see_more_pill"
            android:visibility="gone" />

        <!-- MŨI TÊN GỢI Ý LƯỚT (SWIPE HINT) -->
        <ImageView
            android:id="@+id/ivMoreHint"
            android:layout_width="48dp"
            android:layout_height="68dp"
            android:layout_gravity="center_vertical|end"
            android:layout_marginEnd="13dp"
            android:alpha="0.8"
        android:src="@drawable/ic_chevron_right_white_24" />

    </FrameLayout>

</com.google.android.material.card.MaterialCardView>
